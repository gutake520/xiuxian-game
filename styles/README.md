# 样式目录

目前扩展仍加载根目录 `style.css`，本目录尚未接入。

迁移时按需要新增 `base.css`（配色、文字、基础组件）、`shell.css`（面板与导航）、`home.css`、`character.css`、`inventory.css`、`map.css`、`sects.css` 等页面样式。不要现在复制旧样式，也不要依靠不断追加覆盖规则维护同一个组件。

新增文件必须明确接入加载链；搬迁一组规则时同步移除旧定义并检查酒馆主题与手机布局。
